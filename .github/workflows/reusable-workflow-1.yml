name: Reusable workflow example

on:
  workflow_call:

jobs:
  reuse1job:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: List Files
      run: |
        pwd
        ls -l
        echo "done"
